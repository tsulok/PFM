<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/default.margin"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/categoryItemImageButtonRoot"
        style="@style/AppTheme.Category_Button"
        android:layout_margin="8dp">

        <ImageButton
            android:id="@+id/categoryItemImageButton"
            android:layout_width="match_parent"
            android:background="@null"
            android:layout_height="match_parent"
            android:src="@drawable/ic_category_food" />

    </FrameLayout>

    <com.pinup.pfm.ui.widget.ExtendedTextView
        android:id="@+id/categoryItemName"
        style="@style/AppTheme.BaseText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/default.margin"
        android:text="Restaurant" />

</LinearLayout>